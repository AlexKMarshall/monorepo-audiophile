{
  "name": "@audiophile/shop-test",
  "version": "0.0.0",
  "scripts": {
    "start:server": "pnpm --filter shop start",
    "test": "playwright test",
    "playwright:install": "pnpm exec playwright install --with-deps"
  },
  "dependencies": {
    "shop": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "1.34.0",
    "@types/node": "20.0.0",
    "typescript": "5.0.4"
  }
}
